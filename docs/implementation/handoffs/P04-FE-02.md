# P04-FE-02 handoff — Credentials UI

- Reference: `docs/screens/lumi_models_routing.webp`; visual source `DESIGN.md`.
- Added one-time credential creation form, owner/mode selection, provider binding, validation/error/loading states, masked fingerprint display, status/last-used metadata, and explicit rotate/revoke actions in `apps/web/src/features/models/models-routing-panel.tsx`.
- No secret redisplay/reveal control exists; revoke presents a consequence confirmation and all mutations are CSRF/idempotency protected.
- Evidence: web typecheck/build and API secret-redaction smoke assertions. The panel conditionally loads credential and usage data by role so members/viewers are not forced through unauthorized requests; it re-fetches on role changes and reuses idempotency keys across retryable form submissions.
