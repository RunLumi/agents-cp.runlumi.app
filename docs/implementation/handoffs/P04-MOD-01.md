# P04-MOD-01 handoff — Provider/model/alias catalog

- Contract: `p04-cg-v1`, freeze commit `57b2df9`.
- Implemented stable provider/model/alias descriptors, capabilities, lifecycle, policy allowlists, deterministic IDs, and vendor-neutral validation in `apps/api/src/modules/catalog.rs` and `apps/api/src/routes/ai_catalog.rs`.
- Added catalog D1 persistence and additive lifecycle/policy APIs. Disabling a provider/model/route filters new selection without rewriting historical usage.
- Route creation provisions a missing global alias in the same D1 batch, preventing an unusable route/alias split.
- Evidence: `cargo test -p lumi-agents-control-plane-api --locked --lib` (131 tests), P04 smoke catalog/lifecycle/policy checks, and `wrangler d1 migrations apply` on a fresh persist directory.
- Follow-up boundary: P03 remains owner of trusted project/device policy transport; P04 consumes its typed extension without inventing membership semantics, accepts the frozen opaque schema-0 model placeholder without widening policy, and validates organization/project scope when present.
