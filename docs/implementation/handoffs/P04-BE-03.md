# P04-BE-03 handoff — Provider adapters and SSRF boundary

- Contract: `p04-cg-v1`.
- Added vendor-neutral OpenAI-compatible and Anthropic translation boundaries plus deterministic development mock streams (pre-output failure, success, post-output failure, timeout, and downstream-abort fixture) in `apps/api/src/adapters/providers.rs`; Anthropic request tools, non-stream JSON, and SSE events are normalized before the shared decoder.
- Server-controlled endpoints require exact allowlisted hosts, HTTPS (HTTP only in explicit development), no userinfo/query/fragment, private/link-local/loopback rejection, and redirect errors.
- Adapter errors normalize status/body without forwarding raw upstream text; provider request IDs and usage are extracted at the boundary.
- Evidence: provider unit tests, P04 SSRF hostile assertion, timeout/abort signal wiring, and Worker WASM build.
