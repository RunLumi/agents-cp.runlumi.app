# Work Packet Handoff — P05-QA-01

## What changed

Added `apps/api/scripts/p05-smoke.mjs` and the P05 integration-gate evidence artifacts. The smoke provisions two tenants/users/devices/projects, runs a real managed mock inference, exercises budget/rate/accounting, privileged approval/result/replay, MCP expansion, stale policy, retry/cancel, hostile tenant/device cases, timeline/audit correlation, and hard-budget exhaustion against a fresh D1/Worker.

## Evidence

- `node --check apps/api/scripts/p05-smoke.mjs`: passed.
- `pnpm exec oxfmt --check apps/api/scripts/p05-smoke.mjs`: passed.
- `node apps/api/scripts/p05-smoke.mjs`: exit `0`, 185 checks passed, 0 required failures, 4 explicit limitations.
- Evidence: `docs/implementation/evidence/P05-IG-2026-09-25.md`.
- Gate: `docs/implementation/gates/P05-IG.md` is CONDITIONAL PASS.

## Accepted limitations

- No public browser/computer execution endpoint; CUA remains unavailable by design.
- No public capability-definition write route; browser-shaped smoke fails closed without a private D1 seed.
- Local workerd did not persist `request_cancelled` after passive disconnect; the smoke makes no cancellation claim.
- Accounting/rate hardening follow-ups are recorded in the gate rather than hidden.

## Downstream notes

- Do not replace the smoke's direct D1 evidence queries with client-side state.
- Do not treat a browser approval response as proof that a browser action ran.
- Re-run with `P05_REQUIRE_CANCELLATION_ROW=1` only when a runtime is expected to persist cancellation rows.
