# Work Packet Handoff — P05-FE-02

## What changed

Added lazy Tools & Approvals UI with capability/risk matrix, MCP registrations, browser/computer policy, exact-binding approval queue, Base UI confirmation flows, and fail-closed/review states.

## Reference/evidence

- `DESIGN.md`, `docs/screens/lumi_models_routing.webp`, and `docs/screens/lumi_account.webp` informed navigation/table/security-event treatment.
- No reference screen exists for the approval card or capability matrix; those surfaces follow the frozen contract and existing panel patterns.
- Web typecheck/lint/build and 36 web tests passed; browser capture was unavailable.

## Downstream notes

- Approval actions require server confirmation and never provide a client-side force-allow.
- Secret handles and arbitrary metadata are display-only/opaque; raw arguments are never rendered.
