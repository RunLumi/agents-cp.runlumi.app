# P04-BE-05 handoff — Provider health/cooldown

- Contract: `p04-cg-v1`.
- Added D1 provider health persistence and tenant-scoped reads in `apps/api/src/repositories/ai.rs`; gateway records normalized success/failure and cooldown state.
- Credential failures are tenant-scoped degraded health and do not globally disable a provider. Cooldown and policy/lifecycle filtering occur before dispatch.
- Evidence: P04 smoke observes a successful fallback and catalog health response; D1 shows success/failure/timeout counters and TTFT/completion latency totals, with schema/index checks passing on a fresh database.
