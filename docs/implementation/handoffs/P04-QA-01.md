# P04-QA-01 handoff — Streaming/fallback/security matrix

- Contract: `p04-cg-v1`.
- `apps/api/scripts/p04-smoke.mjs` covers managed credential creation, local-only registration, two candidates, streaming output, pre-output fallback, non-stream JSON, post-output no-fallback, read timeout classification, usage attribution, route publish/rollback/history, stale version rejection, idempotent replay/conflict, policy, revoked credentials, capability mismatch, SSRF, guessed cross-tenant access, and secret redaction.
- Rust unit suite covers retry/fallback lifecycle, SSE split/EOF/error handling, capability/policy filtering, credential ownership, crypto envelope redaction, bounded token estimates, SSRF/private destination validation, weighted/fixed strategy semantics, and the complete P04 outbox event registry. The P03 policy consumer now fails closed for a present-but-malformed model section.
- Evidence: fresh D1 migration, local Worker smoke JSON success, D1 lifecycle/reservation queries, 131 Rust tests, clippy with `-D warnings`, WASM check, production dry-run, and web typecheck/build. The smoke performs a real first-chunk downstream abort; local workerd did not deliver a cancellation D1 row, so signal/drop finalization is recorded as a platform follow-up rather than claimed as passed.
- Browser visual capture was completed with authenticated Chrome CDP at desktop and narrow widths; the connected Review-browser focus harness itself remains unavailable, so that specific focus traversal is not claimed.
