# P04-INT-01 handoff — LumiAgents managed inference path

- Contract: `p04-cg-v1`, F26, and the P04-INT-01 write surface.
- `docs/contracts/inference-v1.md` defines explicit opt-in managed mode: P02 session + one trusted `X-Org-ID`, alias-only requests, no provider credential/endpoint fields, cancellation without retry, and local/BYOK coexistence when policy permits. The merged P03 route and policy snapshot are now consumed server-side; the gateway resolves the latest trusted P03 snapshot after authorization and validates its org/project scope.
- The managed gateway is exercised through the real native and OpenAI-compatible endpoints in `apps/api/scripts/p04-smoke.mjs` against the same combined Worker/D1 persist as the 16-check P03 smoke.
- Evidence: P04 smoke uses an authenticated Lumi-style client and stable aliases; no client release is needed after route rollback. P03's device/project/policy slice and the P04 model-policy view now run on one merged router; the policy response includes both the versioned P03 snapshot and P04 model-policy fields, and P04 resolves snapshots server-side. The external LumiAgents repository has a runtime-only managed inference config/mapping helper; full desktop rollout remains a downstream integration step.
