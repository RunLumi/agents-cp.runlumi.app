# Work Packet Handoff — P05-INT-02

## What changed

Added strict managed tool-decision protocol contracts and terminal decision ports while preserving local permission semantics as a separate unmanaged path. The agent-side execution gate is designed to run after normalized input/hooks and before the handler.

## Verification

- Protocol schema assertions and targeted TypeScript/lint checks passed in the external repository.

## Known limitations

- The host adapter must be constructed with the control-plane client before managed execution can be enabled; missing gates fail closed when managed mode is selected.
