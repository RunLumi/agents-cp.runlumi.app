# P04-INT-02 handoff — Provider/model mapping

- Contract: `p04-cg-v1`.
- `docs/contracts/provider-mapping-v1.md` defines the one-way ZCode/LumiAgents identity boundary, stable IDs/aliases, unknown-ID fail-closed behavior, and local-secret non-upload rule.
- `apps/api/src/modules/catalog.rs` keeps provider/model/alias identity vendor-neutral and validates lifecycle/capabilities; no client-supplied provider/model/credential ID is treated as authorization.
- Evidence: catalog/inference smoke and cross-tenant tests; the local `RunLumi/LumiAgents` checkout now contains `packages/provider/src/lumi-managed-inference.ts`, a runtime-only stable-alias/control-plane config boundary and explicit server-owned legacy identity resolver. The mapping remains one-way and unknown identities fail closed; P08 still owns migration UX and rollout.
