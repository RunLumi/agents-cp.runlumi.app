# P04-MOD-03 handoff — Route compiler/selector

- Contract: `p04-cg-v1`.
- Implemented immutable route config validation, fixed/ordered/weighted-health strategies, capability/policy/lifecycle/health filtering, deterministic weighted ordering, and explicit capability mismatch errors in `apps/api/src/modules/routing.rs`.
- Added route draft/publish/history/rollback, version hashes, tenant checks, alias provisioning, and lifecycle API in `apps/api/src/routes/ai_catalog.rs` and `apps/api/src/repositories/ai.rs`.
- Evidence: P04 domain tests, fresh D1 migration, route publish/rollback smoke, and D1 history verification.
