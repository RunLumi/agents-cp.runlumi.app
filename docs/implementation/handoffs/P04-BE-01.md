# P04-BE-01 handoff — Catalog and route APIs

- Contract: `p04-cg-v1`.
- Added provider/model/alias catalog reads and mutations, credential metadata APIs, route draft/publish/history/rollback, lifecycle mutations, policy get/update, cursor pagination, central P02 authorization, CSRF, and P01 error envelopes.
- D1 repository uses bound SQL and invariant batches; route versions are protected by immutable triggers.
- Evidence: P04 smoke creates two candidates, publishes immutable versions from draft rows, rejects stale writes, rolls back, reads history, updates policy, and exercises cross-tenant denial. Scoped idempotency replay/conflict and conditional version guards are asserted for provider/model lifecycle, route, policy, credential revoke, and rotation paths; failed pre-commit claims are released.
