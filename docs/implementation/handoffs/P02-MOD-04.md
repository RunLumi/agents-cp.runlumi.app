# Work Packet Handoff — P02-MOD-04

## What changed
Added the central `authorize(principal, org, membership, permission, resource)` decision with default deny, role grants, org state checks, active membership checks, and resource scope validation.

## Contracts consumed
P02-CG permissions, default roles, denial reasons, and explicit org context.

## Contracts produced
Stable `Permission`, `DenyReason`, `AuthorizationDecision`, and pure policy tests.

## Database/migration impact
None; repositories provide current membership snapshots.

## Tests added
Owner/admin/member/viewer, unknown permission, stale membership, suspended org, unverified mutation, and cross-tenant resource tests.

## Known limitations
Project/team fine-grained policy is P03+; P02 only establishes org/resource scope.

## Downstream agent notes
Every org route must call `routes::authorization::authorize_org`; do not add handler role comparisons.

## Do not assume
An active session does not imply active membership or permission.

## Follow-up packets
All P02 protected API/FE/QA packets and P03/P04.
