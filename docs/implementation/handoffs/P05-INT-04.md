# Work Packet Handoff — P05-INT-04

## What changed

Added versioned browser/computer policy contracts and fail-closed gates at the protocol browser broker, desktop host bridge, and CUA broker. Managed requests require a valid policy decision; cancellation remains available when the gate is unavailable.

## Verification

- External protocol/host/CUA tests and targeted typecheck/lint passed.

## Known limitations

- The public Computer Use package remains an API-compatible unavailable placeholder; permission gating does not make CUA executable.
- The host still needs a concrete gate adapter and managed context propagation from the control-plane device transport.
