# P05 Integration Gate Evidence — 2026-09-25

## Reproduction

```text
node apps/api/scripts/p05-smoke.mjs
```

Observed against a fresh local D1 persist and a self-started Wrangler Worker:

- exit code: `0`
- migrations: `0001` through `0010`
- checks: `185 passed`
- required failures: `0`
- limitations: `4`
- generic privileged tool approval/result/replay: passed
- MCP expansion denial: passed
- stale-policy denial: passed
- hard-budget denial after a successful managed request: passed
- retry/cancel and cross-tenant/device negatives: passed
- run timeline, audit, and request/run correlation: passed
- passive disconnect: no `request_cancelled` D1 row observed; no cancellation claim made

The fresh persist directory used for the diagnostic was
`/var/folders/zz/jzz3w1rj5lq21d_7c0nkc31m0000gn/T/lumi-p05-smoke-w0i6Jy`
and is intentionally temporary; the smoke's redacted JSON report is the
reviewable evidence, not a persisted database fixture.

## D1 evidence queries

The smoke executed tenant-scoped queries for:

- `inference_requests` by request ID, checking org/project/run/device/principal,
  managed alias, and selected route/version;
- `budget_reservations` by request ID, checking run/budget/hold identity;
- `usage_events` and `cost_records` by request/usage identity;
- `run_events` by run ID and event sequence;
- `security_events` by organization/run/request correlation;
- usage denial projections by organization.

No raw prompt, tool argument, secret, device token, or cookie value was
written to this evidence file.

## Accepted limitations

1. The Worker has no public browser/computer execution route. The external
   LumiAgents host seams are present, but public CUA is an unavailable
   placeholder; only policy/result metadata is claimed.
2. No public capability-definition write route exists, so a fresh D1 has no
   platform browser capability row. Browser-shaped evaluation fails closed
   with `capability_not_defined`; no direct D1 seed was used.
3. Local workerd did not persist `request_cancelled` after the passive client
   abort. The smoke reports the absence and does not infer success.
4. The managed reservation currently represents the bounded P04 token estimate
   in the minor-unit field; actual cost is appended through reconciliation.
   Atomic rate counters and priced token conversion remain follow-up hardening.

## Other verification

- `cargo test --workspace --lib`: 243 passed.
- `cargo clippy --workspace --all-targets --locked -- -D warnings`: passed.
- `cargo check --workspace --target wasm32-unknown-unknown --locked`: passed.
- Web typecheck, lint, 39 web tests, and production build: passed.
- Desktop review browser was disconnected; no screenshot or focus-traversal
  evidence is claimed.
