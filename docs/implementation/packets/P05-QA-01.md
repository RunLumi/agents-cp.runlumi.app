# Work Packet — P05-QA-01: Managed control-loop integration and hostile matrix

## Metadata

- Phase: P05
- Lane: QA
- Status: ready
- Owner: P05 coordinator / QA agent
- Depends on: P05-CG, P05-BE-01..04, P05-FE-01..03, P05-INT-01..04
- Blocks: P05 integration gate and phase exit
- Specs: F04, F08, F10, F12, F13, F16, F21, F23
- ADRs: ADR 0002, ADR 0004, ADR 0005
- Contract version / Contract Gate commit: `p05-cg-v1` / `b5a5ea8`

## Outcome

Prove one real managed run and the mandatory hostile matrix against a fresh combined D1/Worker environment, with evidence sufficient for a repeatable P05 Integration Gate.

## In scope

- `apps/api/scripts/p05-smoke.mjs` and deterministic fixtures.
- Cross-tenant, stale membership/device/policy, transition/retry/cancel, budget concurrency, approval, MCP expansion, revoked-device, and stream-disconnect reconciliation cases.
- Browser captures/focus evidence for P05 screens where the review browser is available.
- P05 Integration Gate artifact and handoff evidence.

## Explicitly out of scope

- Product implementation changes outside a reproduced failing boundary, and unsupported claims about passive workerd disconnect delivery.

## Write surface

Allowed paths:

```text
apps/api/scripts/p05-smoke.mjs
apps/api/src/modules/p05_domain_tests.rs
apps/api/src/routes/p05_tests.rs
docs/implementation/gates/P05-IG.md
docs/implementation/evidence/p05-*
docs/implementation/handoffs/P05-QA-01.md
```

Shared files requiring coordinator approval: root/package scripts only if a new smoke entry point is required.

## Frozen contracts consumed

- All P05-CG APIs, entities, permissions, events, and policy schema.
- P03 device/project/policy routes and P04 inference/usage/route contracts.
- P01/P02 error, idempotency, cursor, and authorization behavior.

## Implementation notes

Use fresh D1 migrations `0001`–`0010` and deterministic development fixtures. Every negative test must substitute a real cross-tenant ID or stale identity, not merely omit a field. Do not claim `request_cancelled` unless a lifecycle row is observed.

## Acceptance criteria

- [ ] Enrolled active device starts a managed run with stable org/project/device/session/run propagation.
- [ ] Inference uses the managed alias and creates a budget reservation.
- [ ] Privileged browser/tool action returns approval-required and cannot proceed before web approval.
- [ ] Approval resolution is audited and the action event appears in the timeline.
- [ ] Usage reconciles exactly once and cost retains pricing version.
- [ ] Hard budget blocks the next eligible request.
- [ ] Cross-tenant, revoked-device, stale-policy, MCP expansion, double cancel/retry, and concurrent reservation cases fail closed.
- [ ] `pnpm check`, `pnpm build`, WASM check, fresh migration, and smoke all pass.

## Tests

- [x] unit
- [x] integration
- [x] cross-tenant negative
- [x] browser/UX
- [x] performance/regression
- [x] not applicable with reason

## Security checklist

- [x] tenant scope preserved
- [x] authorization centralized
- [x] no secret/token leakage
- [x] no raw sensitive body logging
- [x] idempotency/concurrency considered
- [x] destructive action behavior understood
- [ ] not applicable with reason

## Handoff

Before PR review, complete `templates/handoff.md`; the Integration Gate must include exact commands, output, screenshots, and limitations.

## Stop conditions

Stop and escalate if a hostile case passes because a client-supplied ID was treated as authority, a budget race overspends, a denied tool proceeds, or evidence depends on an unobserved runtime event.
