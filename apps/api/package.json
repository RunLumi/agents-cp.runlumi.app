{
  "name": "@runlumi/agents-cp-api",
  "version": "0.0.0",
  "private": true,
  "scripts": {
    "dev": "wrangler dev --env development",
    "build": "wrangler deploy --dry-run --env production",
    "deploy": "wrangler deploy --env production",
    "db:migrations:apply:local": "wrangler d1 migrations apply DB --local --env development",
    "db:migrations:list:local": "wrangler d1 migrations list DB --local --env development",
    "smoke:local": "node scripts/smoke-local.mjs"
  },
  "devDependencies": {
    "wrangler": "4.137.0"
  }
}
